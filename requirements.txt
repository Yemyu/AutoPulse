# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# HTTP 请求 (舆情爬虫)
requests>=2.28.0

# 中文 NLP (分词 + 情感分析)
jieba>=0.42.0
snownlp>=0.12.0

# 机器学习 (影响因子回归 / Bootstrap)
scikit-learn>=1.3.0

# 可视化 (BI 图表)
matplotlib>=3.7.0

# 网页看板为纯静态站点 (HTML + ECharts)，由 GitHub Pages 托管，无需 Python 服务端依赖

# 销量预测建模 (阶段三)
statsmodels>=0.14.0        # ARIMA
xgboost>=2.0.0             # XGBoost (macOS 需额外 brew install libomp)
prophet>=1.1.0             # Prophet (首次运行需安装 cmdstan 后端)
torch>=2.0.0               # LSTM (全局 + 车系嵌入)
